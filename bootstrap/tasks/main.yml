###################################################################################################

- name: pip | dependencies

  with_items: '{{ bootstrap_pip_dependencies }}'

  pip : name={{ item }}

  tags:
    - dockable
    - bootstrap
    - pip

###################################################################################################
